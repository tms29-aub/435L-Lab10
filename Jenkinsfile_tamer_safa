pipeline {
    agent any
    stages {
        stage('Run in Docker') {
            steps {
                script {
                    docker.image('ubuntu').inside {
                        // Run a shell command inside the container
                        echo 'Running command inside Ubuntu container'
                        sh 'echo Hello from inside the container!'
                    }
                }
            }
        }
    }
}