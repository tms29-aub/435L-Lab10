def runShellCommand(command) {
    def process = command.execute()
    process.waitFor()
    return process.text
}

pipeline {
    agent any
    stages {
        stage('Run Custom Shell Command') {
            steps {
                script {
                    def result = runShellCommand("echo Hello, custom command!")
                    echo result
                }
            }
        }
    }
}