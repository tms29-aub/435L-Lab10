pipeline {
    agent any
    stages {
        stage('Run Custom Shell Command') {
            steps {
                script {
                    def result = sh(script: 'echo Hello, custom command!', returnStdout: true).trim()
                    echo result
                }
            }
        }
    }
}
