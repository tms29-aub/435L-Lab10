pipeline {
    agent any
    environment {
    PATH = "/bin:/usr/bin:${env.PATH}"
    }

    stages {
        stage('Check PATH') {
            steps {
                sh 'echo $PATH'
            }
        }
    }
}
